---
galaxy_info:
  role_name: xray_exit
  namespace: chain_proxy
  author: chain_proxy
  description: Legacy Xray exit-node role (kept for rollback; not in active plays)
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - "24"

dependencies:
  - role: xray
